# Review - repository-quality-gates - 2026-07-15

## Scope

- Branch: `feature/repository-quality-gates`
- Base reviewed: `main...HEAD`
- Commits reviewed: `c9b4576` through `8a3a203`, plus earlier branch-only commits included in the PR diff
- Diff stat: 47 files changed, 7047 insertions, 314 deletions
- Feature size / route: S / quick

## Verification

| Gate | Result | Notes |
|---|---|---|
| `npm run verify` | PASS | Completed in about 127 seconds in this environment; this exceeds the <=60 second warmed-machine NFR target and should be watched separately from the blocking findings below. |
| `npm run test:e2e` | NOT PASSED | First sandboxed run failed before Vite startup due sandbox file access. Escalated run timed out after about 7 minutes before returning a pass/fail result. |

## Findings

- **[stage-1] PR gate can fail on Linux due case-mismatched import** - `src/components/Snake.tsx:5`; AC: AC-03, AC-07; problem: the import uses `snakeTail/SnakeTail`, but the tracked component path is `src/assets/snakeModel/snakeTail/snakeTail.tsx`, so Windows can pass while the Ubuntu GitHub Actions checkout can fail module resolution; resolution: Fix now - restore the import to tracked casing or commit a real case-only rename so the tracked path matches.
- **[stage-1] Level balance changed in infrastructure feature** - `src/engine/levels/levels.json:4`; AC: n/a; problem: this feature explicitly excludes gameplay and level-balance changes, but level time and lives were changed; resolution: Fix now - restore the original level JSON values or move the balance change to an approved gameplay feature.
- **[stage-1] Runtime debug key changes production controls** - `src/index.ts:64`; AC: n/a; problem: the branch adds a production `L` / `l` key listener that logs snake state, which changes runtime/control behavior outside the CLI/worker quality-gates scope; resolution: Fix now - remove the listener or keep diagnostics in the Playwright harness/dev-only code path.
- **[stage-1] Generated Playwright HTML report is tracked** - `playwright-report/index.html:3`; AC: AC-05; problem: generated Playwright output is committed even though artifact hygiene says `playwright-report/` stays out of Git; resolution: Fix now - remove it from the index and rely on `.gitignore`.
- **[stage-1] Generated Playwright run state is tracked** - `test-results/.last-run.json:1`; AC: AC-05; problem: generated test-result state is committed even though artifact hygiene says `test-results/` stays out of Git; resolution: Fix now - remove it from the index and rely on `.gitignore`.

## Verdict

CHANGES REQUESTED

